name: rai-toolkit
about: Calculate and conflate multiple roadnetworks, calculating RAI
version: 0.1.0

subcommands:
    - addresses:
        about: Address Validation
        args:
            - output:
                long: output
                short: o
                value_name: OUTPUT
                about: Output file to write to
                takes_value: true
                required: false
            - input:
                long: input
                short: i
                value_name: INPUT
                about: Input file to read from
                takes_value: true
                required: false
